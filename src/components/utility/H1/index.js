import styled from 'styled-components';

const H1 = styled.h1`
  color: ${props => props.color};
  font-size: 2em;
  margin-bottom: 0.25em;
  margin-left: 5vw;
  margin-right: 5vw;
`;

export default H1;
